## 1. プロジェクト準備

- [x] 1.1 React と TypeScript の開発環境を用意し、開発サーバーで初期画面を表示できることを確認する
- [x] 1.2 Todo の識別子、タイトル、完了状態を表す型と localStorage のキーを定義し、TypeScript の型チェックが通ることを確認する

## 2. Todo 状態と永続化

- [x] 2.1 `useState` で Todo 一覧を初期化し、localStorage に保存済みの一覧があれば復元できることをブラウザで確認する
- [x] 2.2 Todo 一覧の変更を localStorage に保存し、保存値がない場合や不正な JSON の場合に空の一覧で起動できることを確認する
- [x] 2.3 Todo を追加、完了状態を切替、削除する状態更新を実装し、それぞれが期待する一覧を生成することを確認する

## 3. 画面と操作

- [x] 3.1 Todo の入力欄と追加操作を実装し、有効なタイトルでは未完了の Todo が追加され、空白のみでは追加されないことを確認する
- [x] 3.2 Todo 一覧に完了状態を切り替える操作と削除操作を実装し、操作結果が画面に反映されることを確認する

## 4. 動作確認

- [x] 4.1 アプリをビルドし、TypeScript のエラーなく完了することを確認する
- [x] 4.2 Todo の追加、完了切替、削除の後にページを再読み込みし、一覧と完了状態が復元されることを確認する
